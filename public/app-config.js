// Per-build overrides. The web build ships this empty; app builds (Capacitor)
// overwrite their bundled copy to set a default relay server, e.g.:
//   window.VC_DEFAULT_SERVER = 'xxxx.trycloudflare.com';
